<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.medicine.dao.IEnterDao">
	<select id="login" parameterType="User" resultType="UserView">
		select
		user_id as id,user_name as name,user_password as password from t_users u where 
		u.user_name = #{name} and
		u.user_password = #{password}
	</select>
	<select id="checkLoginName" parameterType="java.lang.String" resultType="java.lang.String">
		select user_name from t_users where user_name = #{lgName}
	</select>
</mapper>